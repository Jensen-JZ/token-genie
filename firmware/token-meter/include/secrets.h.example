// Copy to secrets.h and fill in. secrets.h is gitignored.
#pragma once

#define WIFI_SSID  "your-wifi"
#define WIFI_PASS  "your-password"

// Your token-meter host endpoint + access key (must match host/config.json auth_key).
#define METER_URL  "https://your-host.example.com/usage"
#define METER_KEY  "your-access-key"
